[package]
name = "test-page-selection"
version = "0.1.0"
edition.workspace = true

[package.metadata.typst-bake]
template-dir = "./templates"
fonts-dir = "../fonts"

[dependencies]
typst-bake = { workspace = true, features = ["full"] }
