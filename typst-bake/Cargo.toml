[package]
name = "typst-bake"
version = "0.1.0"
edition = "2021"
description = "Bake Typst templates, fonts, and packages into your binary for offline PDF generation"
license = "MIT OR Apache-2.0"

[dependencies]
typst-bake-macros = { path = "../typst-bake-macros" }
typst = "0.14"
typst-pdf = "0.14"
typst-as-lib = "0.15"
include_dir = "0.7"
